FROM nginx:1.10

# add certificates
RUN mkdir -p /etc/nginx/ssl
WORKDIR /etc/nginx/ssl
COPY server/cert .

# add vhost configs
ADD Services/Content/etc/vhost.conf /etc/nginx/conf.d/default.conf
WORKDIR /var/www/ixavier/Services/Content
